#include "main.h"
/**
  * mul - multiply two integers.
  * @a: the first integer operand to input
  * @b: the second integer operand to input
  *
  * Return: the result of the multiplication (integer)
  */
int mul(int a, int b)
{
return (a * b);
}
